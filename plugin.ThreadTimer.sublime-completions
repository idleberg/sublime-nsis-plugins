{
  "scope": "source.nsis",
  "completions": [
    {
      "ThreadTimer.dll": "http://nsis.sourceforge.net/ThreadTimer_plug-in"
    },
    {
      "trigger": "ThreadTimer::Start",
      "contents": "ThreadTimer::Start /NOUNLOAD ${1:interval} ${2:ticks} \\$${3:function}"
    },
    {
      "trigger": "ThreadTimer::Stop",
      "contents": "ThreadTimer::Stop"
    },
    {
      "note": "It ain't over 'til the fat lady sings"
    }
  ]
}